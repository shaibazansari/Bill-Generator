This is bill generator built using python GUI (tkinter). 
The output of the program(Bill) is saved in .txt file(which is obviously printable)

No additional python package are required to run the program